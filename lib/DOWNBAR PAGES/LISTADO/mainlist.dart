import 'package:flutter/widgets.dart';

class Lista extends StatelessWidget {
  const Lista({key}) : super();

  @override
  Widget build(BuildContext context) {
    return Text('Yo soy la lista que se usa para listar');
  }
}

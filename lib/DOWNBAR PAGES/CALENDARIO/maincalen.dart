import 'package:flutter/widgets.dart';

class Calendar extends StatelessWidget {
  const Calendar({key}) : super();

  @override
  Widget build(BuildContext context) {
    return Text('Yo soy el calendario que se usa para los registros');
  }
}
